# Testing

## Summary
- Served the static site locally using Python's built-in HTTP server.
- Verified the home page responds successfully via HTTP request.

## Commands
1. Start server: `python -m http.server 8000`
2. Request page: `curl -I http://localhost:8000/index.html`
3. Stop server after verification.
