# PayrollPro HR System

This repository hosts the PayrollPro HR System static site.

## Branches
- **main**: default branch tracking the production-ready version of the site.
- **work**: active development branch that should be merged into `main` as updates are finalized.

To update `main` with the latest changes from `work`, run:
```bash
git checkout main
git merge work
```
